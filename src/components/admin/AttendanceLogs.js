import React,{useCallback,useEffect,useState}from"react";import{RefreshCw,Search}from"lucide-react";import{getAttendanceReportLogsApi,getAttendanceSummaryApi}from"../../services/adminService";import{formatDateTime}from"../../utils/orderDisplay";import"./AttendanceLogs.scss";
const duration=(m)=>`${Math.floor(Number(m||0)/60)}h ${Number(m||0)%60}m`;const AttendanceLogs=()=>{const[tab,setTab]=useState("summary");const[data,setData]=useState({summaries:[],logs:[],page:1,totalPages:1});const[filters,setFilters]=useState({search:"",dateFrom:"",dateTo:"",status:"all"});const[input,setInput]=useState("");const[page,setPage]=useState(1);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const load=useCallback(async()=>{setLoading(true);setError("");try{const params={...filters,page,limit:20};const response=tab==="summary"?await getAttendanceSummaryApi(params):await getAttendanceReportLogsApi(params);if(response?.EC!==0)throw new Error(response?.EM);setData(response.DT)}catch(e){setError(e?.EM||e?.message||"Attendance report could not be loaded.")}finally{setLoading(false)}},[filters,page,tab]);useEffect(()=>{load()},[load]);const submit=(e)=>{e.preventDefault();setPage(1);setFilters((v)=>({...v,search:input.trim()}))};return <main className="attendance-report"><header><div><h1>Attendance Reports</h1><p>Shifts are attributed by check-in date. Full overnight duration remains with that date.</p></div><button onClick={load}><RefreshCw size={16}/>Refresh</button></header><div className="attendance-tabs"><button className={tab==="summary"?"active":""} onClick={()=>{setTab("summary");setPage(1)}}>Staff summary</button><button className={tab==="logs"?"active":""} onClick={()=>{setTab("logs");setPage(1)}}>Shift logs</button></div><div className="attendance-report-toolbar"><form onSubmit={submit}><Search size={16}/><input value={input} onChange={(e)=>setInput(e.target.value)} placeholder="Search staff"/></form><input type="date" value={filters.dateFrom} onChange={(e)=>{setPage(1);setFilters((v)=>({...v,dateFrom:e.target.value}))}}/><input type="date" value={filters.dateTo} onChange={(e)=>{setPage(1);setFilters((v)=>({...v,dateTo:e.target.value}))}}/>{tab==="logs"&&<select value={filters.status} onChange={(e)=>{setPage(1);setFilters((v)=>({...v,status:e.target.value}))}}><option value="all">All shifts</option><option value="completed">Completed</option><option value="open">Open</option></select>}</div>{loading?<div className="attendance-report-state">Loading report...</div>:error?<div className="attendance-report-state error">{error}</div>:tab==="summary"?<div className="attendance-report-table"><table><thead><tr><th>Staff</th><th>Shifts</th><th>Completed</th><th>Open</th><th>Total hours</th><th>Average shift</th></tr></thead><tbody>{data.summaries?.length?data.summaries.map((row)=><tr key={row.id}><td><strong>{row.full_name}</strong><small>{row.email}</small></td><td>{row.shift_count}</td><td>{row.completed_shift_count}</td><td>{row.open_shift_count}</td><td>{duration(row.total_worked_minutes)}</td><td>{duration(row.average_completed_shift_minutes)}</td></tr>):<tr><td colSpan="6">No staff records found.</td></tr>}</tbody></table></div>:<div className="attendance-report-table"><table><thead><tr><th>Staff</th><th>Check in</th><th>Check out</th><th>Status</th><th>Duration</th></tr></thead><tbody>{data.logs?.length?data.logs.map((row)=><tr key={row.id}><td><strong>{row.User?.full_name}</strong><small>{row.User?.email}</small></td><td>{formatDateTime(row.check_in_time)}</td><td>{row.check_out_time?formatDateTime(row.check_out_time):"—"}</td><td>{row.is_open?"Open":row.has_invalid_duration?"Invalid":"Completed"}</td><td>{row.has_invalid_duration?"Invalid":row.is_open?"In progress":duration(row.worked_minutes)}</td></tr>):<tr><td colSpan="5">No shift logs found.</td></tr>}</tbody></table></div>}<footer><button disabled={page<=1} onClick={()=>setPage((v)=>v-1)}>Previous</button><span>Page {data.page||page} of {Math.max(data.totalPages||1,1)}</span><button disabled={page>=(data.totalPages||1)} onClick={()=>setPage((v)=>v+1)}>Next</button></footer></main>};export default AttendanceLogs;
